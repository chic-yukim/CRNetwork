<?xml version="1.0" encoding="utf-8"?>
<ParameterContainer>
  <Info>
    <Container name ="Network@Socket@UDP_ControlRecv" version="0.1"/>
  </Info>
  <ParameterList>
    <parameter name="NetworkType" value="Socket_UDP"/>
    <parameter name="ServerIP" value="192.168.100.25"/>  <!-- only for client , for binding -->
    <parameter name="SocketPort" value="9402"/> <!-- only for client-->
    <parameter name="NetworkBufferSize" value="10240"/> <!--byte -->
    <parameter name="SocketBufferSize" value="8192"/> <!-- byte -->
    <parameter name="Nodelay" value="0"/> <!-- 1: noDelay set, 0 : None -->
	  <parameter name="SendTimeout" value="100"/>  <!-- ms -->
	  <parameter name="RecvTimeout" value="100"/>  <!-- ms -->
    <parameter name="KeepAlive" value="1"/>    <!-- 1 : enable, 0 : disable -->
    <parameter name="ImmediatelyDisconnect" value="0"/>    <!-- 1 : enable, 0 : disable -->
    <parameter name="SendRetry" value="0"/>    <!-- count -->
  </ParameterList>
</ParameterContainer>

