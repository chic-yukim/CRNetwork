<?xml version="1.0" encoding="utf-8"?>
<ParameterContainer>
  <Info>
    <Container name ="Network@Socket@UDP_MotionSend" version="0.1"/>
  </Info>
  <ParameterList>
    <parameter name="NetworkType" value="Socket_UDP"/>
    <parameter name="ServerIP" value="161.122.113.179"/>     <!-- only for client,  connected client IP-->
    <parameter name="SocketPort" value="8313"/> <!-- only for client-->
	  <parameter name="NetworkBufferSize" value="1024"/> <!-- byte -->
    <parameter name="SocketBufferSize" value="8192"/> <!-- byte -->
    <parameter name="Nodelay" value="0"/> 
	  <parameter name="SendTimeout" value="10"/>  <!-- ms -->
	  <parameter name="RecvTimeout" value="10"/>  <!-- ms -->
    <parameter name="KeepAlive" value="0"/> <!-- 1 : enable, 0 : disable -->
    <parameter name="ImmediatelyDisconnect" value="0"/>    <!-- 1 : enable, 0 : disable -->
    <parameter name="SendRetry" value="0"/>    <!-- count -->
  </ParameterList>
</ParameterContainer>

